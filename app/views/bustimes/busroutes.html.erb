<ol class="breadcrumb">
  <li><%= link_to "Home", root_path %></li>
  <li class="active">Choose a Route</li>
</ol>

<ul class="list-group">
  <% @busroutes.each do |l| %>
    <li class="list-group-item">
      <%= link_to l['rt'] + ' ' + l['rtnm'], busdirections_path(l['rt'], l['rtnm']) %>
    </li>
  <% end %>
</ul>
